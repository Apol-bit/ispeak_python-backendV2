# Speech model goes here

Copy the complete exported ONNX Whisper model into this directory. At minimum,
the loader expects `config.json` and one or more `*.onnx` files, plus the model's
tokenizer and processor files.
